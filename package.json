{
  "name": "temporal-bridge",
  "version": "1.0.0",
  "description": "AI memory system that creates searchable, temporal knowledge graphs from Claude Code conversations",
  "private": true,
  "scripts": {
    "dev:cli": "pnpm --filter temporal-bridge-cli dev",
    "dev:mcp": "pnpm --filter temporal-bridge-cli dev:mcp",
    "build": "pnpm --filter temporal-bridge-cli build",
    "test": "pnpm --filter temporal-bridge-cli test",
    "test:watch": "pnpm --filter temporal-bridge-cli test:watch",
    "test:coverage": "pnpm --filter temporal-bridge-cli test:coverage",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "typecheck": "pnpm --filter temporal-bridge-cli typecheck",
    "cli": "pnpm --filter temporal-bridge-cli start",
    "start:mcp": "pnpm --filter temporal-bridge-cli start:mcp"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.2",
    "@golevelup/ts-vitest": "^0.5.0",
    "@types/node": "^22.7.7",
    "@vitest/ui": "^2.1.2",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}